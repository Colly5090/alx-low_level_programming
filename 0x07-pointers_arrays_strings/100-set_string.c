#include "main.h"

/**
 * set_string - sets a pointer to a character
 * @s: a pointer pointing the the character
 * @to: character to be pointed
 * Return: 0 always success
 */

void set_string(char **s, char *to)
{
	*s = to;
}
