#include "main.h"
/**
  * _isupper - an entry point that prints uppercase letter
  *@c: character to be checked
  * Return: 1 for uppercase and 0 for otherwise
  */

int _isupper(int c)
{

if (c >= 65 && c <= 90)
return (1);
else
return (0);

}
